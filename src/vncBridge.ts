/**
 * @deprecated — import from './vnc' instead.
 * Kept as a re-export shim for backward compatibility.
 */
export { VncBridge, VncSession, KEYSYM } from './vnc';
export type { VncRect, VncInfo }         from './vnc';
